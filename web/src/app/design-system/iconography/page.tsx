"use client";

import * as foundation from "../foundationCommon.css";

export default function IconographyPage() {
  return (
    <div className={foundation.page}>
      <header className={foundation.header}>
        <h1 className={foundation.title}>Iconography</h1>
        <p className={foundation.subtitle}>Icon set is not yet defined.</p>
      </header>

      <section className={foundation.surface}>
        <div className={foundation.textMuted}>Too be defined.</div>
      </section>
    </div>
  );
}
